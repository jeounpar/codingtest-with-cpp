#include <bits/stdc++.h>

using namespace std;
int n, m, k;
int ans = 0;

int main(void)
{
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cin >> n >> m >> k;
	while (k--)
	{
		int r, c;
		cin >> r >> c;
		for (int i = 0; i < r ; i++)
		{
			
		}
	}

	cout << ans << "\n";
	return 0;
}